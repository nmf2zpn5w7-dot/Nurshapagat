<%- include('partials/head') %>

<main class="site-wrap" style="padding: 4rem 0; text-align: center;">
    <h1 style="font-size: 4rem; color: var(--accent);"><%= title %></h1>
    <p style="font-size: 1.5rem; color: var(--muted); margin-bottom: 2rem;"><%= pick(locale, "Страница не найдена", "Бет табылмады") %></p>
    <a href="/" class="btn-accent" style="display: inline-block; padding: 1rem 2rem; background: var(--accent); color: white; border-radius: 12px; font-weight: 700;"><%= pick(locale, "Вернуться на главную", "Басты бетке қайту") %></a>
</main>

<%- include('partials/foot') %>
